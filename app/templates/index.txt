<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <title>Neural Style Transfer</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            background: url("{{ url_for('static', filename='img/background.png') }}") no-repeat center center fixed;
            background-size: cover;
            font-family: 'Segoe UI', sans-serif;
            color: white;
            text-align: center;
        }

        .overlay {
            background-color: rgba(0, 0, 0, 0.6);
            height: 100vh;
            width: 100vw;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }

        h1 {
            font-size: 3rem;
            margin-bottom: 2rem;
        }

        .button-group {
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        a button {
            padding: 1rem 2rem;
            font-size: 1.2rem;
            border: none;
            border-radius: 12px;
            background-color: #ffffffaa;
            color: #000;
            cursor: pointer;
            transition: all 0.2s ease-in-out;
        }

        a button:hover {
            background-color: #fff;
            transform: scale(1.05);
        }

        .note {
            margin-top: 2rem;
            font-size: 1rem;
            font-style: italic;
            color: #ddd;
        }
    </style>
</head>
<body>
    <div class="overlay">
        <h1>Modunuzu Seçin</h1>

        <div class="button-group">
            <a href="{{ url_for('mod1') }}"><button>Mod1: Elle Stil Yükle</button></a>
            <a href="{{ url_for('mod2') }}"><button>Mod2: Stil Kütüphanesinden Seç</button></a>
            <a href="{{ url_for('mod3') }}"><button>Mod3: Canlı Kamera ile Stil Aktarımı</button></a>
        </div>

        <div class="note">
            <p><strong>Mod Açıklamaları:</strong><br>
            <strong>Mod1:</strong> İçerik ve stil görsellerini sen seç.<br>
            <strong>Mod2:</strong> Sadece içerik yükle, stili sanatçılardan seç.<br>
            <strong>Mod3:</strong> Kameranı aç, anı sanatla yakala! (anlık aktarım).</p>
        </div>
    </div>
</body>
</html>
